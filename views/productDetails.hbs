    <section class="bg-light">
        <div class="container pb-5">
            <div class="row">
                <div class="col-lg-5 mt-5">
                    <div class="card mb-3">
                        <img class="card-img img-fluid" src="{{ProductosDB.thumbnail}}" alt="Card image cap" id="product-detail">
                    </div>
                </div>
                <!-- col end -->
                <div class="col-lg-7 mt-5">
                    <div class="card">
                        <div class="card-body">
                            <h1 class="h2">{{ProductosDB.name}}</h1>
                            <p class="h3 py-2">${{ProductosDB.price}}</p>
                            <h6>Descripcion:</h6>
                            <p>{{ProductosDB.description}}</p>
                            <ul class="list-inline">
                                <li class="list-inline-item">
                                    <h6>Categoria :</h6>
                                </li>
                                <li class="list-inline-item">
                                    <p class="text-muted"><strong>{{ProductosDB.category}}</strong></p>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script src="{{@root.foo}}/assets/js/ProductFunctions.js"></script>